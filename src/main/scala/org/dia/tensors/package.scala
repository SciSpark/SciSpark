package org.dia

/**
 * Provides data structure objects, classes, and traits for storing tensors (i.e. arrayed objects that are mathematical
 * coordinates in space).
 *
 * Currently there are two main representation schemes, each with its own strengths: the ND4J and the Breeze library
 * data structures.
 */
package object tensors {

}
